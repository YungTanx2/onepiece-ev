# Copy to .env for local development (never commit the real .env)

# Port the Express server listens on (Railway injects this automatically)
PORT=3006

# Path to the SQLite price-history database.
# Local: leave unset to use ./price-history.db
# Railway: set to /data/price-history.db (persistent volume mounted at /data)
DB_PATH=./price-history.db
